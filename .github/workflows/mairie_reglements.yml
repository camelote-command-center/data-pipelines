name: Mairie réglements parser (GE)

on:
  workflow_dispatch:
    inputs:
      reason:
        description: "Reason for manual run"
        required: false
        default: "manual test"

concurrency:
  group: mairie-reglements-parser
  cancel-in-progress: false

jobs:
  crawl-and-upsert:
    name: Crawl GE commune websites and upsert to re-LLM
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r pipelines/mairie-reglements/requirements.txt

      - name: Run mairie-reglements parser
        env:
          RE_LLM_DB_URL: ${{ secrets.RE_LLM_DB_URL }}
        run: python pipelines/mairie-reglements/parser.py

      - name: Update dataset metadata
        if: always()
        run: |
          COUNT=$(curl -s \
            -H "apikey: $KEY" -H "Authorization: Bearer $KEY" \
            -H "Accept-Profile: bronze_ch" \
            -H "Prefer: count=exact" \
            --head "$URL/rest/v1/mairie_reglements_ge?select=*" \
            | grep -i content-range | sed 's|.*/||' | tr -d '\r')
          STATUS_PAYLOAD="{\"record_count\":${COUNT:-0}"
          if [ "${{ job.status }}" = "success" ]; then
            STATUS_PAYLOAD="${STATUS_PAYLOAD},\"last_acquired_at\":\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\",\"last_error\":null,\"status\":\"active\"}"
          else
            STATUS_PAYLOAD="${STATUS_PAYLOAD},\"last_error\":\"Workflow failed at $(date -u +%Y-%m-%dT%H:%M:%SZ) — check Actions log\",\"status\":\"error\"}"
          fi
          curl -s -X PATCH "$CMD_URL/rest/v1/datasets?code=eq.ge_mairie_reglements" \
            -H "apikey: $CMD_KEY" -H "Authorization: Bearer $CMD_KEY" \
            -H "Content-Type: application/json" -H "Prefer: return=minimal" \
            -d "$STATUS_PAYLOAD"
        env:
          URL: ${{ secrets.RE_LLM_SUPABASE_URL }}
          KEY: ${{ secrets.RE_LLM_SUPABASE_SERVICE_ROLE_KEY }}
          CMD_URL: ${{ secrets.CAMELOTE_DATA_SUPABASE_URL }}
          CMD_KEY: ${{ secrets.CAMELOTE_DATA_SUPABASE_SERVICE_KEY }}

      - name: Upload parser log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: mairie-reglements-log-${{ github.run_id }}
          path: pipelines/mairie-reglements/*.log
          if-no-files-found: ignore
          retention-days: 14

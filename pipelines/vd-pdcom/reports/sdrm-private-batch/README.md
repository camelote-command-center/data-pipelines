# SDRM historical private contour batch

Sixteen source contours from document `860247ca-7943-5cfd-a948-7f1d70fed5d9`, SHA256 `a267218ebe407b251ddf4d8e5d13c89b1ff68b2a41086d60476ad8bad66f3b67`, page 13, map dated 10 July 2014. These are historical landscape/planning outlines, not current available capacity or surveyed legal boundaries. Source planning/procedure semantics are retained individually. All rows remain `review_required` and `internal_review_only`; commune completion is unchanged.

Four predeclared training identities fit a similarity transform. All four reserved identities remain in validation: errors 1.296, 5.479, 6.364 and 3.150 m; RMSE 4.531283 m. The fit is coordinate recovery from generalized historical map footprints, not a surveyed precision certificate. Archived imagery requested at 2011/2014 has actual central flight vintages 2009/2012. Selected identity and boundary sheets support manual review; raw reference URLs and response hashes remain in JSON. No rejected earlier fit is used as validation proof.

The bundle covers D2, E1, E2, E3, Ei1, L2, L3, L4, L5, L6, L7, P1, P2, P3, P4 and P5. Their complete source geometries lie within the independent control hull. Twenty original official parcel snapshots support the wider 17-site audit; this deliverable excludes L9 because an original cadastral ring is invalid. Eleven additional source contours outside/partly outside support remain excluded. No invalid geometry is repaired and no border fragments are clipped. D2/L7 retain Préverenges attribution and Ei1 retains Morges attribution. P2/P4 include built areas; woodland/stream margins in other contours are not deducted or interpreted as capacity.

The frozen fixture contains 139 positive site/EGRID pairs across 130 EGRIDs. The adapter refreshes exact official references and rejects changed membership, geometry or type. It checks the stored PDF hash/page, source transform, hull support, historical semantics and replay values before using existing private views and registered receiver routes. Reruns reuse deterministic sector IDs. A changed hash-bound fixture requires renewed review.

Run `python -m unittest discover -s pipelines/vd-pdcom/tests -p test_sdrm_historical_pilot.py` with the existing pipeline dependencies. Full acquisition/review evidence is also linked from domain discovery attempt `b52a7deb-d77c-4f1e-ba52-be2d0d69e505`, operation `ed8d3a74-1ff5-4428-b603-6318a647f7fe`.

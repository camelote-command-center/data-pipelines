# Eysins official sources and embedded-table discovery fix

The municipal website publishes document links inside escaped JSON `data-entities` attributes. Ordinary DOM anchor selection sees none of those links. The updated crawler reads only `name` and `_downloadBtn` fields as inert HTML, retains row labels/referring pages, and applies existing URL/crawl guards. Expansion is bounded by500KB per table,1MB per page and1,000 rows, with diagnostic reasons on malformed/limited data. No JavaScript is evaluated. Numeric /_doc/ downloads from pages whose h1 explicitly identifies a PDCom become pending landing candidates; PDF content is not assumed before review.

Real table fixtures recover8construction-page links and19communal-regulation links. They include zoning/regulation sources, not a newly established PDCom. Three byte-frozen adjacent sources are retained here: a single raster PGA map printed2November1997 (publication labels1999), a20-page scanned Plan des zones bundle, and16-page construction/territorial regulation amendments with signed2017approval/entry-into-force evidence. These are not added to the PDCom acquisition count or treated as new development sectors. The first map has a visible coordinate grid, north arrow and historical buildings useful for a possible reference bridge to the SDAN image. Detailed correspondence/currentness work remains pending.

`source-review.json` contains original/final official URLs, hashes, dimensions, page metadata and review limits. The regulation cover states council adoption28June2017, preliminary cantonal approval24August2017 and entry into force3November2017; do not replace current RDPPF/special-plan checks with the historical general regulation. The20-page bundle is not yet fully reviewed.

Validation:44parser tests, including embedded plan/row context, numeric downloads, regulation rejection, malformed JSON, unsafe URLs and limits. `verify.py` reproduces the real fixture link counts and frozen PDF hashes/page counts. A one-time retry eligibility update for the243currently unresolved communes is tracked in the operational receipt; statuses remain unresolved and processing uses the existing bounded hourly queue. No duplicate workflow or claim of completed retries.

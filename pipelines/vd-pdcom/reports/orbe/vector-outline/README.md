# Orbe main-map source vectors

SHA-gated PDF1 extraction preserves one priority-densification territory as inner and outer band edges. Two closed valid nested rings match the preceding even-odd red fill; they are not two sectors. The separate1:5000 centre inset is excluded from the1:10000 main map.

The mixed-use legend color produces eight raw path parts: six valid filled polygons (including tiny fragments) and two degenerate parts. Keep raw points; no automatic grouping or minimum-area deletion. Visibility and overprint review remain pending. Orange/hatching categories are not extracted.

All geometry is in PDF points, not a geographic CRS. Exact reproduction and visual overlay verified. No runtime sectors/parcel intersections or receiver changes. Next align using source building/reference correspondences with held-out checks, resolve fragments and other categories, then evaluate the cartographic band sensitivity in parcel joins.
